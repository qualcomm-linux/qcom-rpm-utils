# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause
name: "Upload RPMs to Artifactory"
description: "Uploads built RPM/SRPM packages to a JFrog Artifactory repository."


inputs:
  server_url:
    description: "Artifactory server URL"
    required: false
    default: "https://qartifactory.qualcomm.com/"

  target_repo:
    description: "Target YUM/RPM repo name, optionally with a path prefix"
    required: false
    default: "qsc-rpm-releases-stage"

  rpm_dir:
    description: "Directory containing the built *.rpm / *.src.rpm files to upload"
    required: false
    default: "output"

  target_subpath:
    description: "Subpath under target_repo to upload RPMs into (e.g. the package name)."
    required: false
    default: ""

  cache_uploads_manifest:
    description: >
      Optional path to a TAB-separated manifest of source tarballs to cache back:
      each line is 'localpath<TAB>cache-relative-path'. Uploaded under
      <target_repo>/sources/<cache-relative-path>. Produced by resolve-sources.sh
      --emit-cache-uploads.
    required: false
    default: ""

  provenance_info:
    description: "Extra Artifactory properties as a JSON object (e.g. {\"commit\":\"...\",\"workflow\":\"...\"})"
    required: false
    default: "{}"

  access_token:
    description: "Pre-generated Artifactory access token. Used directly when qsc_api_key is not set, and as a fallback if the qsc_api_key exchange fails."
    required: false

  qsc_api_key:
    description: "QSC API key. When set, it is exchanged for a temporary Artifactory access token (takes precedence over access_token)."
    required: false

  access_token_url:
    description: >
      QSC endpoint that exchanges qsc_api_key for an Artifactory token.
    required: false
    default: "https://api.qualcomm.com/softwarecenter/v1/chipSoftware/artifactory/tokens"

  access_token_data:
    description: "Access token data for generating access token - used in conjunction with qsc_api_key"
    required: false
    default: "{\"softwareName\":\"LU.QCLINUX\",\"softwareType\":\"SoftwareImage\",\"releaseTag\":\"2.0\"}"

runs:
  using: "composite"
  steps:

    - name: Install dependencies if needed
      shell: bash
      run: |
        set -euo pipefail
        SUDO=sudo
        if [ "$(id -u)" = 0 ]; then SUDO=""; fi

        required=(jq curl)
        missing=()
        for pkg in "${required[@]}"; do
          if ! command -v "$pkg" >/dev/null 2>&1; then
            missing+=("$pkg")
          fi
        done

        if ((${#missing[@]})); then
          echo "Installing missing packages: ${missing[*]}"
          if command -v dnf >/dev/null 2>&1; then
            $SUDO dnf install -y "${missing[@]}"
          elif command -v apt-get >/dev/null 2>&1; then
            export DEBIAN_FRONTEND=noninteractive
            $SUDO apt-get update -y -qq
            $SUDO apt-get install -y -qq --no-install-recommends "${missing[@]}"
          else
            echo "ERROR: no supported package manager (dnf/apt-get) to install: ${missing[*]}" >&2
            exit 1
          fi
        else
          echo "All prerequisites already installed."
        fi

    - name: Setup JFrog CLI
      uses: jfrog/setup-jfrog-cli@v5
      with:
        version: 2.113.0

    - name: Resolve Artifactory access token
      id: gen_token
      shell: bash
      env:
        ACCESS_TOKEN_URL: ${{ inputs.access_token_url }}
        QSC_API_KEY: ${{ inputs.qsc_api_key }}
        ACCESS_TOKEN: ${{ inputs.access_token }}
        ACCESS_TOKEN_DATA: ${{ inputs.access_token_data }}
      run: |
        set -euo pipefail

        if [ -z "$QSC_API_KEY" ] && [ -z "$ACCESS_TOKEN" ]; then
          echo "::error::Neither qsc_api_key nor access_token provided. Set the" \
               "QSC_API_KEY or ARTIFACTORY_ACCESS_TOKEN secret on the calling repo." >&2
          exit 1
        fi

        emit_token() {
          echo "::add-mask::$1"
          echo "access_token=$1" >> "$GITHUB_OUTPUT"
        }

        # QSC key takes precedence: exchange it for a temporary token. When it
        # is absent, fall back to the pre-generated access_token as-is.
        if [ -z "$QSC_API_KEY" ]; then
          echo "No qsc_api_key provided; using the pre-generated access_token."
          emit_token "$ACCESS_TOKEN"
          exit 0
        fi

        echo "Generating access token using qsc_api_key..."
        if [ -n "$ACCESS_TOKEN" ]; then
          echo "Note: both credentials are set; qsc_api_key takes precedence." \
               "The access_token is kept only as a fallback."
        fi

        http_code=""
        body=""
        if response=$(curl -sS \
          -w '\n%{http_code}' \
          --retry 3 --retry-delay 2 --max-time 60 \
          -X POST "$ACCESS_TOKEN_URL" \
          -H 'Content-Type: application/json' \
          -H 'X-QCOM-TokenType: apikey' \
          -H "Authorization: $QSC_API_KEY" \
          -d "$ACCESS_TOKEN_DATA"); then
          http_code="${response##*$'\n'}"
          body="${response%$'\n'*}"
          echo "Token endpoint HTTP status: ${http_code}"
          echo "Token endpoint response (token redacted):"
          echo "$body" | jq 'if has("token") then .token = "***REDACTED***" else . end' 2>/dev/null \
            || echo "$body" | sed -E 's/"token"[[:space:]]*:[[:space:]]*"[^"]*"/"token":"***REDACTED***"/g'
        else
          echo "Token endpoint request failed (curl exit $?)."
        fi

        token="$(printf '%s' "$body" | jq -r '.token // empty' 2>/dev/null || true)"

        if [ -n "$token" ]; then
          emit_token "$token"
          echo "Access token generated successfully."
          exit 0
        fi

        # Exchange failed. Fall back to the pre-generated token when one exists,
        # so a misconfigured or not-yet-live QSC key cannot block a release.
        if [ -n "$ACCESS_TOKEN" ]; then
          echo "::warning::Could not obtain a token from qsc_api_key (HTTP ${http_code:-n/a})." \
               "Falling back to the pre-generated access_token."
          emit_token "$ACCESS_TOKEN"
          exit 0
        fi

        echo "::error::Failed to generate an access token from qsc_api_key"
        echo "HTTP status: ${http_code:-n/a}"
        exit 1

    - name: Configure JFrog server
      shell: bash
      env:
        SERVER_ID: "artifactory-server"
        SERVER_URL: ${{ inputs.server_url }}
        ACCESS_TOKEN: ${{ steps.gen_token.outputs.access_token }}
      run: |
        set -euo pipefail
        jf c remove "$SERVER_ID" --quiet 2>/dev/null || true
        jf c add "$SERVER_ID" \
          --url "$SERVER_URL" \
          --access-token "$ACCESS_TOKEN" \
          --interactive=false \
          --overwrite
        echo "JFrog CLI configured with server ID: $SERVER_ID"
        echo "Server URL: $SERVER_URL"

    - name: Upload RPMs with architecture detection
      shell: bash
      env:
        SERVER_ID: "artifactory-server"
        TARGET_REPO: ${{ inputs.target_repo }}
        TARGET_SUBPATH: ${{ inputs.target_subpath }}
        RPM_DIR: ${{ inputs.rpm_dir }}
        PROVENANCE_INFO: ${{ inputs.provenance_info }}
        MANIFEST: ${{ inputs.cache_uploads_manifest }}
      run: |
        set -euo pipefail

        shopt -s nullglob
        rpms=()
        if [ -d "$RPM_DIR" ]; then
          rpms=("$RPM_DIR"/*.rpm)
        fi

        if [ "${#rpms[@]}" -eq 0 ]; then
          if [ -n "$MANIFEST" ]; then
            echo "No RPMs in '$RPM_DIR'; skipping RPM upload (cache-back only)."
            exit 0
          fi
          echo "ERROR: no *.rpm files found in '$RPM_DIR'" >&2
          exit 1
        fi

        PROPS=""
        for key in $(echo "$PROVENANCE_INFO" | jq -r 'keys[]'); do
          val=$(echo "$PROVENANCE_INFO" | jq -r --arg k "$key" '.[$k]')
          PROPS="${PROPS:+${PROPS};}${key}=${val}"
        done

        TARGET_DIR="$TARGET_REPO/"
        if [ -n "$TARGET_SUBPATH" ]; then
          TARGET_DIR="${TARGET_REPO}/${TARGET_SUBPATH}/"
        fi
        echo "Upload target: $TARGET_DIR"

        for f in "${rpms[@]}"; do
          base="$(basename "$f")"
          arch="${base%.rpm}"; arch="${arch##*.}"
          fileprops="${PROPS:+${PROPS};}rpm.arch=${arch}"

          echo "Uploading $base (arch=${arch}) -> $TARGET_DIR"
          jf rt upload \
            --server-id="$SERVER_ID" \
            --flat=true \
            --target-props "${fileprops}" \
            "$f" \
            "$TARGET_DIR"
        done

    - name: Cache back source tarballs
      if: ${{ inputs.cache_uploads_manifest != '' }}
      shell: bash
      env:
        SERVER_ID: "artifactory-server"
        TARGET_REPO: ${{ inputs.target_repo }}
        MANIFEST: ${{ inputs.cache_uploads_manifest }}
      run: |
        set -euo pipefail

        if [ ! -s "$MANIFEST" ]; then
          echo "No source tarballs to cache back (manifest empty or missing): $MANIFEST"
          exit 0
        fi

        while IFS=$'\t' read -r local rel || [ -n "$local" ]; do
          [ -z "$local" ] && continue
          if [ ! -f "$local" ]; then
            echo "ERROR: cache-back source not found: $local" >&2
            exit 1
          fi
          target="${TARGET_REPO}/sources/${rel}"
          echo "Caching source $local -> $target"
          jf rt upload \
            --server-id="$SERVER_ID" \
            --flat=true \
            "$local" \
            "$target"
        done < "$MANIFEST"
